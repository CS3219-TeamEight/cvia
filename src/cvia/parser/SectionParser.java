package cvia.parser;

public interface SectionParser {

    // Classes implementing this will assume that the correct section is passed to them
    // could make parsers singleton components since there will be only 1 parser each

}
